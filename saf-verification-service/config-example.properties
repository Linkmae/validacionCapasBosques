# =====================================================
# SAF Verification Service - Configuración Externa
# =====================================================
# Copiar este archivo a /opt/saf/config/verification.properties
# o configurar la ruta mediante variable de entorno:
# export SAF_CONFIG_PATH=/ruta/a/verification.properties

# === Base de Datos de Configuración (saf_interconexion) ===
db.config.url=jdbc:postgresql://localhost:5432/saf_interconexion
db.config.username=saf_app
db.config.password=saf_app_2026

# === Base de Datos de Capas Geográficas (saf_postgis) ===
db.capas.url=jdbc:postgresql://localhost:5432/saf_postgis
db.capas.username=saf_app
db.capas.password=saf_app_2026

# === Servicio Externo de Predios ===
predios.service.url=http://localhost:8080/servicio-soap-predios/PrediosService?wsdl
predios.service.usuario=1750702068
predios.service.clave=1234

# === Configuración de Cache ===
# Tiempo de vida del cache de reglas en minutos
cache.rules.ttl.minutes=5

# === PostGIS ===
# Sistema de coordenadas por defecto
postgis.default.srid=32717

# === Logging ===
# Nivel de detalle: INFO, DEBUG, WARNING, ERROR
log.level=INFO
